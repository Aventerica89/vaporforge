---
const features = [
  'Unlimited cloud sandboxes',
  'Full Linux terminal access',
  'Real-time AI streaming',
  'File upload and download',
  'Session persistence',
  'Mobile-optimized interface',
  'Dark and light themes',
  'PWA with offline support',
  'Priority support',
];
---

<div class="mx-auto max-w-md rounded-2xl p-px"
  style="background: linear-gradient(135deg, rgba(233,69,245,0.4), rgba(29,211,230,0.3), rgba(47,75,192,0.4))">
  <div class="rounded-2xl bg-vapor-card p-8">
    <div class="text-center">
      <span class="font-display rounded-full bg-gradient-to-r from-vapor-accent/10 to-vapor-purple/10 px-4 py-1.5 text-xs font-semibold tracking-widest text-vapor-accent uppercase">
        Pro
      </span>
      <div class="mt-6">
        <span class="font-display text-5xl font-bold cyber-gradient-text">$20</span>
        <span class="text-vapor-muted">/month</span>
      </div>
      <p class="mt-2 text-sm text-vapor-muted">
        Everything you need to code from anywhere.
      </p>
    </div>

    <ul class="mt-8 space-y-3">
      {features.map((feature) => (
        <li class="flex items-start gap-3 text-sm text-vapor-muted">
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24"
            fill="none" stroke="currentColor" stroke-width="2.5"
            stroke-linecap="round" stroke-linejoin="round"
            class="mt-0.5 shrink-0 text-vapor-purple">
            <polyline points="20 6 9 17 4 12" />
          </svg>
          {feature}
        </li>
      ))}
    </ul>

    <button
      disabled
      class="mt-8 w-full rounded-lg bg-vapor-purple-bg px-6 py-3 text-sm font-semibold text-vapor-muted cursor-not-allowed font-display tracking-wide"
    >
      Coming Soon
    </button>

    <p class="mt-4 text-center text-xs text-vapor-muted">
      Requires a Claude Pro or Max subscription from Anthropic.
    </p>
  </div>
</div>

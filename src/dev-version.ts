// Dev build number — increment before every deploy.
// This is the single source of truth for the backend.
// The frontend has its own copy at ui/src/lib/dev-version.ts
// (both must be bumped together).
export const DEV_BUILD = 1;
